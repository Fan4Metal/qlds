# Normalize line endings to LF in the repo and working tree. Essential for the
# shell scripts and config files that are bind-mounted into Linux containers —
# CRLF would break them.
* text=auto eol=lf
*.sh text eol=lf
*.cfg text eol=lf
*.txt text eol=lf
